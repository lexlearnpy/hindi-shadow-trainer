# 🇮🇳 印地语影子跟读训练器 (Hindi Shadow Trainer)

一个用于练习印地语发音的命令行工具，集成Whisper语音识别和SM-2间隔重复算法。

A CLI tool for practicing Hindi pronunciation with Whisper ASR and SM-2 spaced repetition.

## ✨ 功能特性

### 1. 🎙️ 跟读训练 (Shadowing)
- 播放标准音频（TTS或本地文件）
- 录制用户发音
- Whisper语音识别转写
- 发音评分和差异高亮显示

### 2. 📚 生词本复习 (SRS)
- 基于SM-2间隔重复算法
- 每日自动筛选需复习单词
- 支持"完全忘了/模糊/记得/秒杀"评级

### 3. 🗣️ 高质量TTS
- 使用Edge TTS（hi-IN-MadhurNeural）
- 自然的印地语发音

### 4. 🌍 国际化支持
- 中文/英文界面
- 默认中文，可切换

## 🚀 快速开始

### 安装依赖

```bash
# 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### 首次运行

```bash
# 运行主程序
python main.py
```

首次运行时会自动下载Whisper medium模型（约1.5GB）到`models/`目录。

### 环境变量（可选）

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑.env文件配置
# HF_HOME=./models
# WHISPER_MODEL_SIZE=medium
# HINDI_TRAINER_LANG=zh
```

## 📖 使用说明

### 主菜单

1. **开始跟读训练** - 输入印地语文本，跟随练习
2. **生词本复习** - 查看今天需要复习的单词
3. **添加新单词** - 手动添加生词到数据库
4. **查看统计** - 学习进度统计
5. **设置** - 切换语言等

### 跟读流程

1. 输入或粘贴印地语文本
2. 系统播放标准发音（TTS）
3. 倒计时3-2-1
4. 朗读该句子
5. 系统自动识别并评分
6. 红色高亮显示错误，绿色显示正确

### 复习流程

1. 系统显示印地语单词
2. 播放发音
3. 回想中文含义
4. 按回车查看正确答案
5. 选择记忆程度（1-4）
6. 系统自动计算下次复习时间

## 🏗️ 项目结构

```
hindi-shadow-trainer/
├── main.py                 # 主程序入口
├── config.py              # 全局配置（支持i18n）
├── requirements.txt       # 依赖列表
├── .env.example          # 环境变量模板
├── models/               # Whisper模型存储
├── audio_files/          # 预录音频文件
├── temp/                 # 临时文件（录音、TTS）
├── data/                 # SQLite数据库
│   └── vocab.db
└── modules/              # 功能模块
    ├── audio.py          # 音频录制/播放
    ├── tts.py            # Edge TTS
    ├── whisper_engine.py # Whisper语音识别
    ├── database.py       # SQLite数据库管理
    ├── srs.py            # SM-2算法
    ├── scoring.py        # 发音评分
    ├── highlighter.py    # 文本高亮
    ├── shadowing.py      # 跟读模式
    └── review.py         # 复习模式
```

## ⚙️ 配置说明

### 录音时长

默认30秒，根据文本长度自动调整：
```python
AUDIO_DURATION_DEFAULT = 30        # 默认时长（秒）
AUDIO_DURATION_PER_CHAR = 0.3      # 每字符预留时间
```

### SM-2算法间隔

```python
SRS_INTERVALS = [1, 3, 7, 14, 30, 90]  # 天
```

### 评分阈值

```python
SCORE_EXCELLENT = 90
SCORE_GOOD = 70
SCORE_POOR = 50
```

## 🔧 技术栈

- **语音识别**: OpenAI Whisper (medium模型)
- **TTS**: Edge TTS (hi-IN-MadhurNeural)
- **录音**: sounddevice + wavio
- **音频播放**: pydub
- **文本比对**: python-Levenshtein
- **数据库**: sqlite3
- **CLI界面**: rich

## 📝 注意事项

1. **Whisper模型**: 首次运行会自动下载（约1.5GB），请确保网络畅通和磁盘空间
2. **麦克风权限**: 请确保程序有麦克风访问权限
3. **Edge TTS**: 需要网络连接才能生成语音
4. **FFmpeg**: pydub依赖FFmpeg，请确保已安装

## 🐛 故障排除

### 无法访问麦克风
```bash
# 检查麦克风权限
# Windows: 设置 -> 隐私 -> 麦克风
# Linux: 检查pulseaudio/alsa
```

### Whisper模型下载失败
```bash
# 手动设置模型路径
export HF_HOME=/path/to/models
```

### TTS播放失败
```bash
# 安装FFmpeg
# Ubuntu/Debian: sudo apt-get install ffmpeg
# Mac: brew install ffmpeg
# Windows: 下载并添加到PATH
```

## 📄 许可证

MIT License

## 🙏 致谢

- OpenAI Whisper
- Microsoft Edge TTS
- Rich CLI library
